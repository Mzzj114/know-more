---
trigger: always_on
---

Refer to following tips
- use `.\venv\Scripts\python.exe` instead of just `python`
- if some element plus icon like `<el-icon><ChatDotRound /></el-icon>` doesn't load and browser throw warning, use `<el-icon><chat-dot-round /></el-icon>` instead to solve the issue
